# Runtime dependencies for the Lambda container image (Issue #42).
# Mirrors the runtime deps in pyproject.toml (kept in sync manually).
beautifulsoup4>=4.15.0
httpx>=0.28.1
openai>=2.44.0
pydantic>=2.13.4
pydantic-settings>=2.14.2
python-dotenv>=1.2.2
pyyaml>=6.0.3
